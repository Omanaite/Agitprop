// jest.setup.ts
// Adds custom matchers like toBeInTheDocument(), toHaveTextContent(), etc.
// These come from @testing-library/jest-dom and make assertions readable.
import "@testing-library/jest-dom";
